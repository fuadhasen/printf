#ifndef MAIN
#define MAIN

#include <stdarg.h>

#include <unistd.h>
#include <stdarg.h>
#include <stdio.h>
int _printf(const char *format, ...);
int _putchar(char c);
int print_number(int n);
int _p(const char *format, ...);

#endif
